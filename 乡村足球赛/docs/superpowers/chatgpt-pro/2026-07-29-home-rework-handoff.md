# ChatGPT Pro 外部工程协作记录：横版首页玩法分组与赛前设置

## 本地基线

- Git 提交：`8aa653cd8f219d6adda1c0601a6478f6176aa015`（`fix(minigame): stabilize runtime subpackage startup`）
- 分支：`main`
- 打包时间：2026-07-29 09:05（Asia/Shanghai）
- 源码包：`/tmp/animal-football-home-rework-8aa653c.zip`
- 压缩包大小：53,260 bytes
- SHA-256：`9e37c812826438af01e57698e3b84682bd15fb410617a31494f937319beae2af`

## 打包范围与安全检查

压缩包仅含本任务所需的源码、测试、`README.md`、`package.json`、小游戏入口配置以及两份相关设计文档。明确未包含 `.git`、`node_modules`、`generated`、运行时资源、构建产物、缓存、服务端、数据库、浏览器状态、`.env` 或项目配置中的凭据。

上传前按 API Key、Secret、密码、私钥、Cookie、Authorization 与 Token 等模式扫描。命中内容均为防止凭据入库的说明注释或本地邀请令牌字段名，未发现凭据值。

## 外部工程任务

ChatGPT Pro 对话链接：未创建。本次内置浏览器的 ChatGPT 页面在读取与附件粘贴阶段连续超时；为避免重复开窗、重复发送或误传内容，没有把它的任何输出采纳为代码结论。

原计划任务目标、范围、交付物、禁止项与验收标准仍记录在本次任务说明中：ChatGPT Pro 只能提供可审查的补丁，不提交、不部署、不改线上与不伪称真实环境验证。由于页面未稳定可操作，本轮由 Codex 按同一设计和验收标准独立完成实现。

## 复验记录

### 实际改动

- `src/ui/game-shell.js`
  - 首页进一步收敛为“挑战玩法、立即开赛、战绩与好友”三枚大入口；挑战和社交的具体玩法改在各自二级弹窗展示，首页不再显示阵型预览或阵型按钮。
  - 首页顶部新增双方动物头像、队名与 VS；选队面板加宽，卡片与首页字号放大，难度/时长与主操作整体下移。
  - 新增统一赛前弹窗：左栏为主队阵型球场图与可点的六种阵型，右栏为左手摇杆和右手五个实际操作键的图示；不再用两行文字教学替代手柄。取消直接回到选队。
  - 好友入口先进入赛前弹窗，确认后才创建房间。
- `src/app/main.js`
  - 立即开赛、观看对战、赛季征程、每日挑战和好友邀请统一走“准备比赛 → 赛前弹窗 → 真正开始”的流程。
  - 赛季和每日挑战先生成固定对手及规则，赛前弹窗只允许调整阵型。
- `tools/test-game-shell.mjs`
  - 覆盖首页三大入口、挑战/社交二级弹窗、横版高 DPI 命中、赛前阵型回写、首次教学、二次不重复教学和取消不开始比赛。

### 独立测试

- 本轮布局改造后 `npm run test:game-shell`：通过。覆盖首页三大入口、挑战/社交二级弹窗、横版高 DPI 命中、赛前阵型回写、首次教学状态与取消不开始比赛。
- 本轮布局改造后 `npm run build && npm run verify`：通过。主包约 2.54 MiB，资源分包约 9.17 MiB，静态校验和包体限制通过。
- 上一轮完整 `npm run check`：通过。包含生产构建、静态校验、选项/触控/壳层/运行时分包、好友服务与房间同步、排行榜、赛季、每日挑战、场次闸门和分享卡等测试；本轮尚需在完成视觉验收后再次运行完整总门禁。
- 微信开发者工具实机模拟器复验：通过。工具当前打开的项目目录与本仓库一致，控制台依次出现 `B1_MODULES_REGISTERED modules=143` 与 `HOME_READY devAutoStart=false`；横屏首页正常显示，未再出现 `loadSubpackage:fail module not found`。
- 视觉复验：通过。开发者工具实际横屏渲染确认首页显示双方动物头像 VS、加宽选队面板、下移的三枚大入口与两枚小设置标签；控制台出现 `HOME_READY devAutoStart=false`，无启动错误。
- 发布门禁：原先错误地要求在 `src/data/release-compliance.js` 重复填写微信后台资质，现已移除该本地校验。著作权、出版、软著和小程序备案均以微信后台已提交及平台审核状态为准；本地门禁仅检查代码、包体和首发功能配置，源码中不保存这些资质信息。

### 当前状态

代码与测试修改仅在本地工作区，未提交、未推送、未部署、未执行数据库迁移或线上配置改动。虽然后台资质不再是源码门禁，但仍需完成其余首发风险的复验后，才满足“验收全部通过后提交并推送”的条件。
